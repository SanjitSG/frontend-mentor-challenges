import React from "react";

const RadioInput = ({ data }) => {
  console.log(data);
  // const { id, inputName, label, name, type, value } = radioData;

  return (
    <>
      {/* <input
        type={type}
        name={name}
      />{" "}
      <label htmlFor={id}>{inputName}</label> */}
    </>
  );
};

export default RadioInput;
